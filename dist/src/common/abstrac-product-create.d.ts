export declare abstract class AbstractProductCreate {
    protected formatedSucces(message: string, data: any): {
        message: string;
        data: any;
    };
    protected handleException(error: any): void;
}
